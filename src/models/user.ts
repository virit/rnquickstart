import {ModelType} from "../types/ModelType";

interface StateType {
}

const Model:ModelType<StateType> = {
  namespace: 'user',
  state: {
  },
  effects: {
  },
  reducers: {
  }
};

export default Model;
